var React = require('react');
var createReactClass = require('create-react-class');

var Pieces = createReactClass({
  render: function () {
    return <span>♘</span>;
  }
});

module.exports = Pieces;